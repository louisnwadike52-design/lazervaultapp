import 'package:flutter/material.dart';

/// Shared dark-theme palette for the email feature, matching the Plan My Day
/// screens and the platform dark theme.
class EmailPalette {
  const EmailPalette._();

  static const Color background = Color(0xFF0A0A0A);
  static const Color card = Color(0xFF1F1F1F);
  static const Color divider = Color(0xFF2D2D2D);
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFF9CA3AF);
  static const Color primary = Color(0xFF3B82F6);
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFFB923C);
  static const Color error = Color(0xFFEF4444);
  static const Color accent = Color(0xFF8B5CF6);
}
