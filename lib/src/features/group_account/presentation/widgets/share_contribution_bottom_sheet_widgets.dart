part of 'share_contribution_bottom_sheet.dart';

enum SharePlatform {
  whatsapp,
  telegram,
  email,
  sms,
  copyLink,
  other,
}

enum ShareFormat {
  summaryText,
  pdfReport,
  csvSpreadsheet,
}
