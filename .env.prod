# =====================================================
# LAZERVAULT FLUTTER APP - PRODUCTION CONFIGURATION
# =====================================================
# This file is loaded on PHYSICAL DEVICES only.
# For emulators, .env is used instead.
#
# CI/CD: Replace placeholder values with production secrets
# =====================================================

LIVEKIT_URL=wss://lazervault-sgb9bwog.livekit.cloud
GRPC_API_HOST=https://lazervault-golang-http-805149139138.europe-west1.run.app
GRPC_API_PORT=443
INVOICE_GRPC_HOST=lazervault-golang-http-805149139138.europe-west1.run.app
INVOICE_GRPC_PORT=9090

# ========== CHAT AGENT GATEWAY (Enhanced - Production) ==========
CHAT_GATEWAY_URL=https://chat-gateway.lazervault.com

# ========== BANKING SERVICE (Production) ==========
BANKING_API_URL=https://api.lazervault.app/api/v1

# ========== FLUTTERWAVE CONFIGURATION (V3 API - Production) ==========
# LIVE keys for production (NO "_TEST" in key names)
# Get from: https://dashboard.flutterwave.com → Settings → API Keys
# CI/CD: Inject these as build-time secrets
FLUTTERWAVE_PUBLIC_KEY=${FLUTTERWAVE_PUBLIC_KEY}
FLUTTERWAVE_ENCRYPTION_KEY=${FLUTTERWAVE_ENCRYPTION_KEY}

# ========== MONO CONFIGURATION (Open Banking - Production) ==========
# LIVE keys for production (live_pk_xxx)
# Get from: https://app.withmono.com → Settings → API Keys
# CI/CD: Inject these as build-time secrets
MONO_PUBLIC_KEY=${MONO_PUBLIC_KEY}
# NOTE: MONO_SECRET_KEY is stored server-side only (banking-service)
# Never expose secret keys in client-side code!

# ========== ENVIRONMENT ==========
ENVIRONMENT=production